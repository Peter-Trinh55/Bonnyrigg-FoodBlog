# Copy this to .env and fill values
SECRET_KEY=change-me
DATABASE_URL=sqlite:///site.db

# Optional email (for password reset). If not set, reset emails are disabled.
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=1
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_DEFAULT_SENDER=
